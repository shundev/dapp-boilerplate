# Minimum dApp boilerplate

Minimum dApp boilerplate with Parcel, React, Redux and Truffle

## Installation

* `git clone git@github.com:shunsukeh/dapp-boilerplate.git
* `cd dapp-boilerplate
* `npm install`
* `npm start`
* Go to http://localhost:3000
